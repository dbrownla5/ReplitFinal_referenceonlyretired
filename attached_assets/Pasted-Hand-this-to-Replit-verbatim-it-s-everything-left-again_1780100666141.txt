Hand this to Replit verbatim — it’s everything left against the launch build, which already has the PDF, the links, and the custody form fields done.

For Replit — finish these four, then deploy:

1. Wire the custody fields into the email (netlify/functions/contact.ts).
The contact form already sends pickupMethod, pickupRelease, and courierNotes in the submission, but the function throws them away. Add those three to the payload type and print them inside the existing “Pickup details (Fast Bag Fill)” section of the email body, so Dayna actually sees how the bags are arriving.

2. Add the 5th commission tier on three pages.
The tier is: Low-Value Volume, $5–$10 range, 35% to client / 65% WLC, with the note that it’s reviewed at intake and never applied retroactively.

	•	FastBagFill.tsx — add it to the commission grid, after the Designer & Luxury row.
	•	CuratedResalePillar.tsx — add it to the commission table, after Full Closet Liquidation.
	•	Pricing.tsx — add a line to the resale card’s details list noting the 35/65 low-volume split, reviewed at intake.

3. Attach the agreement to bag/resale emails (contact.ts).
When a submission is a Fast Bag Fill or resale path (bags count or pickup method is present, or the summary mentions Fast Bag / Resale / Consignment / Pickup), append two lines to the email body:

	•	Resale Agreement: https://thewelllivedcitizen.com/WLC-Resale-Agreement.pdf
	•	A line stating the agreement is for review only — signing happens after item evaluation, not at intake.

4. Update the confirmation screen for bag/resale submissions (Contact.tsx, the “Message Sent” success state).
When the submission was a bag/resale path, the success screen should additionally show:

	•	A link to the Resale Agreement PDF, with the line: review it now, you sign at evaluation, not before.
	•	An echo of their pickup details (bags, urgency, windows, method), plus the method-specific note — UPS label path: “you’ll receive a prepaid label via email”; courier path: “courier details confirmed via text.”
	•	For all non-resale submissions, leave the existing “Thank you — I’ll be in touch” screen exactly as-is.

Hard rule across all four: the agreement is never signed at intake — it’s informational until evaluation. Don’t add any auto-sign step.

That’s the full remaining surface. Once those four are in and npm run build is clean, it deploys.